# Xnihilo Homes Full Stack

## Local run

1. Open terminal in the `backend` folder.
2. Create virtual environment:
   python -m venv venv
3. Activate it:
   Windows: venv\Scripts\activate
   Mac/Linux: source venv/bin/activate
4. Install dependencies:
   pip install -r requirements.txt
5. Run:
   python app.py
6. Open:
   http://127.0.0.1:5000

## Demo admin
Username: admin
Password: 1234

## Notes
- SQLite is used for the academic/local version.
- `mysql_schema.sql` is included for a future MySQL/AWS RDS version.
