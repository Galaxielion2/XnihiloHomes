document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('productForm');if(!form)return;const params=new URLSearchParams(location.search);const id=params.get('id');let products=getProducts();let editing=products.find(p=>p.id==id);if(editing){['name','type','category','price','reservation','status','image','description'].forEach(k=>{const el=document.getElementById(k);if(el)el.value=editing[k]||'';});}
form.addEventListener('submit',e=>{e.preventDefault();const product={id:editing?editing.id:Date.now(),name:name.value.trim(),type:type.value,category:category.value.trim(),price:Number(price.value),reservation:Number(reservation.value||0),status:status.value,image:image.value.trim()||'https://images.unsplash.com/photo-1560518883-ce09059eeffa?auto=format&fit=crop&w=1200&q=80',description:description.value.trim(),location:'Ubicación pendiente',attributes:['Campo editable','Validación frontend','Listado administrativo','Reserva configurable']};if(!product.name||!product.category||!product.price||!product.description){formMessage.textContent='Complete todos los campos requeridos.';formMessage.className='message show';return;}if(editing){products=products.map(p=>p.id===editing.id?product:p);}else{products.push(product);}saveProducts(products);formMessage.textContent='Listado guardado correctamente.';formMessage.className='message show';setTimeout(()=>location.href='producto.html',800);});});
