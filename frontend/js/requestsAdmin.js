document.addEventListener("DOMContentLoaded",async()=>{ const box=document.getElementById("requests-table"); if(!box) return; requireAdmin(); try{ const rows=await apiGet("/api/requests"); box.innerHTML=`<table><thead><tr><th>ID</th><th>Cliente</th><th>Email</th><th>Total</th><th>Estado</th><th>Fecha</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${r.id}</td><td>${r.customer_name}</td><td>${r.customer_email}</td><td>$${Number(r.total_reservation).toLocaleString()}</td><td>${r.request_status}</td><td>${r.created_at}</td></tr>`).join("")}</tbody></table>`; }catch(err){ box.innerHTML=`<p class="message error">${err.message}</p>`;} });
